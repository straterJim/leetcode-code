class Solution:
    def climbStairs(self, n: int) -> int:
        sum1=0
        sum2=1
        if n == 1:
            return sum2
        for i in range(n):
            sum1,sum2=sum2,sum1+sum2
        return sum2
            
